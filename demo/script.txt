# Press ctrl+j to list dirs and files in fzf.
# Let's cd among some dirs.

# Let's open a file with $EDITOR.

# The plugin config is in $ZT_CONFIG.

# While in fzf, press star (*) to toggle showing
# only the dirs and files listed in $ZT_CONFIG.

# Finally, notice that the config is plain Zsh.
# You can change it as you would any Zsh array.

 vim:tw=50
